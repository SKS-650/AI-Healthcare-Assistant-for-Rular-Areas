"""Speech-to-text abstraction."""

from __future__ import annotations


def transcribe_audio(audio_path: str) -> dict[str, str]:
    """Transcribe an audio file placeholder."""

    return {"audio_path": audio_path, "text": ""}
