# ai_models/training - TODO

- [x] Create `train_disease_model.py` (baseline disease classifier + artifacts)
- [x] Create `train_chatbot.py` (baseline retrieval/chat templates + artifacts)
- [x] Create `train_nlp.py` (baseline NLP preprocessor + optional classifier)
- [ ] (Optional) Run the three scripts and verify `ai_models/saved_models/` artifacts are produced.

