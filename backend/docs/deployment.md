# Backend Deployment

## Overview

The backend is designed for FastAPI deployment with Docker, environment variables, and external services such as Firebase, MongoDB, and Redis.

## Required Environment

- `ENVIRONMENT`
- `JWT_SECRET_KEY`
- `FIREBASE_PROJECT_ID`
- `MONGODB_URL`
- `REDIS_URL`
