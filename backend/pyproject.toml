[project]
name = "ai-healthcare-assistant-backend"
version = "0.1.0"
description = "FastAPI backend for AI Healthcare Assistant for Rural Areas"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
