# TODO - Folder structure creation

- [ ] Verify existing top-level directories match requested structure.
- [ ] Create missing directories (admin_dashboard subtree, project_documentation subtree, datasets subtree, testing subtree, deployment subtree, scripts subtree).
- [ ] Add `.gitkeep` files to ensure empty folders are committed.
- [ ] Verify by re-listing directories.

